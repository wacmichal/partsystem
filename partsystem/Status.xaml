<Window x:Class="partsystem.Status"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:partsystem"
        mc:Ignorable="d"
        Title="Status" Height="598" Width="800">
    <Grid>
        <DataGrid x:Name="StatusDG" HorizontalAlignment="Left" Height="491" Margin="10,68,0,0" VerticalAlignment="Top" Width="774">
            <DataGrid.Columns>
                <DataGridTextColumn  Binding="{Binding id}" x:Name="id" Header="id"/>
                <DataGridTextColumn   Binding="{Binding name}" Header="name" />
                <DataGridTextColumn  Binding="{Binding quanity}" x:Name="quanity" Header="quanity" />
                <DataGridTextColumn   Binding="{Binding quanity_total}" Header="quanity_totl" />

            </DataGrid.Columns>
        </DataGrid>
        <Button Content="DODAJ" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Width="53" Height="53"/>
        <Button x:Name="_closeButton" Content="Zamkij" HorizontalAlignment="Left" Margin="731,10,0,0" VerticalAlignment="Top" Width="53" Height="53" Click="_closeButton_Click"/>

    </Grid>
</Window>
